:ruby
  add_breadcrumb t('view.admin.breadcrumbs.dashboard')

- if logged_in?
  = current_administrator.email
  = current_administrator.universal_id
  = button_to 'ログアウト', admin_session_path, method: :delete
- else
  = link_to 'ログイン', new_admin_session_path
