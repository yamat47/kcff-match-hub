- if current_page.last?
  %span.admin-pagination-last.is-current
    = t('views.pagination.last').html_safe
- else
  = link_to t('views.pagination.last').html_safe, url, { remote:, class: 'admin-pagination-last' }
