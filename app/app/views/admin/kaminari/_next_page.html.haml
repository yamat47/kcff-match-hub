- if current_page.last?
  %span.admin-pagination-next.is-current
    = t('views.pagination.next').html_safe
- else
  = link_to t('views.pagination.next').html_safe, url, { remote:, rel: 'next', class: 'admin-pagination-next' }
